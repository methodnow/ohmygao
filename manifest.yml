---
applications:
  .:
    name: OhMyGao
    framework:
      name: rails3
      info:
        mem: 256M
        description: Rails Application
        exec: 
    url: ${name}.${target-base}
    mem: 1G
    instances: 1
    services:
      OMGDB:
        type: mysql
