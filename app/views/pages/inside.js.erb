$('.maincontent').hide().html('<%= escape_javascript render 'inside' %>').fadeIn(1000);
$('body').removeClass('about why order inside index').addClass('<%= params[:action] %>');
<% unless params[:history].present? %>
  History.pushState({url: "/whatisinside"},"哦买膏 | Oh My Gao","/whatisinside");
<% end %>
