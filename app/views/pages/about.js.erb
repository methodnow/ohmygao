$('.maincontent').hide().html('<%= escape_javascript render 'about' %>').fadeIn(1000);
$('body').removeClass('about why order inside index').addClass('<%= params[:action] %>');
<% unless params[:history].present? %>
History.pushState({url: "/aboutomg"}, document.title,"/aboutomg");
<% end %>
